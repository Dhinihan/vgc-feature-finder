# Segredo para POST /api/cp/import (mesmo valor no deploy e no script local)
CP_IMPORT_SECRET=change-me-to-a-long-random-string

# URL do app hosted (opcional; padrão: vgc-featured-match-finder.lakebed.app)
APP_URL=https://vgc-featured-match-finder.lakebed.app

USE_DEMO_CP_FALLBACK=false
